/**
 * @deprecated `@eclesia/core-modules-pg` was renamed to `@eclesia/cosmos-modules-pg` in 4.0.0,
 * when the engine became chain-agnostic. This package re-exports it unchanged so existing
 * imports keep working for one release; switch your imports, this shim will not be published
 * again.
 */
export * from "@eclesia/cosmos-modules-pg";
