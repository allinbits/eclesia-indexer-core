---
"@eclesia/eclesia-indexer-core": patch
---

Add dependency audit scripts to root package.json for security vulnerability scanning. New scripts include `pnpm audit` for checking dependencies, `pnpm audit:fix` for automatic fixes, and `pnpm outdated` for checking outdated packages.
