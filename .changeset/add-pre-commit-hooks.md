---
"@eclesia/eclesia-indexer-core": patch
---

Add pre-commit hooks using Husky and lint-staged to automatically run ESLint and TypeScript checks on staged files before commits. Helps prevent broken commits and maintains code quality.